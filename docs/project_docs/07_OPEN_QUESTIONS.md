# 07. Decision Log (Closed)

All blocking pre-implementation questions are now resolved.

## Locked Decisions
- Shared global board mode.
- One dice roll per player inside admin-configured roll window.
- Roll window schedule is flexible and configured in admin panel.
- Board is created from scratch in admin panel (no default board).
- Admin manually grants points for real-world sales.
- Active cell action is optional: `buy` or `skip`.
- Secret shop exists and uses same player balance.
- Secret shop limit is `3 items per player per calendar month`.
- Player sees manual accrual updates as in-site notification on login.
- Physical prize fulfillment workflow/statuses are out of MVP scope.

## Status
Project documentation is ready for implementation start.
